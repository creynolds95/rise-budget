// Placeholder until T33 (Vite + React). Proves the workspace link to @rise/shared.
import { categoryMath, type CategoryMath } from '@rise/shared/budget';
import type { Category } from '@rise/shared/schemas';

export type { Category, CategoryMath };
export { categoryMath };
